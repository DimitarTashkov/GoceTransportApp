@page
@using testappHelper.Areas.Identity.Pages.Account.Manage
@model EmailModel
@{
    ViewData["Title"] = Localizer["Manage Email"].Value;
    ViewData["ActivePage"] = ManageNavPages.Email;
}

<h3>
    <i class="fas fa-envelope me-2 text-primary"></i>@Localizer["Manage Email"]
</h3>
<partial name="_StatusMessage" for="StatusMessage" />
<div class="row mt-4">
    <div class="col-md-6">
        <form id="email-form" method="post">
            <div asp-validation-summary="All" class="text-danger mb-3" role="alert"></div>
            @if (Model.IsEmailConfirmed)
            {
                <div class="form-floating mb-3 input-group">
                    <input asp-for="Email" class="form-control" placeholder="Please enter your email." disabled />
                    <div class="input-group-append">
                        <span class="h-100 input-group-text text-success font-weight-bold">
                            <i class="fas fa-check-circle"></i>
                        </span>
                    </div>
                    <label asp-for="Email" class="form-label">
                        <i class="fas fa-envelope-open me-2"></i>@Localizer["Email"]
                    </label>
                </div>
            }
            else
            {
                <div class="form-floating mb-3">
                    <input asp-for="Email" class="form-control" placeholder="Please enter your email." disabled />
                    <label asp-for="Email" class="form-label">
                        <i class="fas fa-envelope me-2"></i>@Localizer["Email"]
                    </label>
                    <button id="email-verification" type="submit" asp-page-handler="SendVerificationEmail" class="btn btn-link">
                        <i class="fas fa-paper-plane me-1"></i>@Localizer["Send verification email"]
                    </button>
                </div>
            }
            <div class="form-floating mb-3">
                <input asp-for="Input.NewEmail" class="form-control" autocomplete="email" aria-required="true" placeholder="Please enter new email." />
                <label asp-for="Input.NewEmail" class="form-label">
                    <i class="fas fa-edit me-2"></i>@Localizer["New Email"]
                </label>
                <span asp-validation-for="Input.NewEmail" class="text-danger"></span>
            </div>
            <button id="change-email-button" type="submit" asp-page-handler="ChangeEmail" class="w-100 btn btn-lg btn-primary">
                <i class="fas fa-save me-2"></i>@Localizer["Change email"]
            </button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
